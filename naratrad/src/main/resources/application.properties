# Nama Aplikasi
spring.application.name=naratrad

# --- ACTIVE PROFILE ---
# Gunakan 'dev' untuk development (H2) atau 'prod' untuk production (Supabase)
# Default: dev
spring.profiles.active=${SPRING_PROFILE:dev}

# --- DATABASE CONFIG ---
# Configuration akan diambil dari application-dev.properties atau application-prod.properties
# Tergantung dari active profile

# --- JPA / HIBERNATE CONFIG ---
# 'update' berarti tabel otomatis dibuat berdasarkan class @Entity
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- FINNHUB EXTERNAL API ---
# Tempel API Key Anda di sini
finnhub.api.key=d518qp9r01qjia5c34agd518qp9r01qjia5c34b0

# --- SWAGGER / OPENAPI ---
# Akses melalui http://localhost:8080/swagger-ui/index.html
springdoc.swagger-ui.path=/swagger-ui.html